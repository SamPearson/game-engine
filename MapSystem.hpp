#ifndef	MAPSYSTEM_HPP
#define	MAPSYSTEM_HPP
#else
#endif


class TownMap
{
	private:
		//Image layers, to be sorted in this order
		BITMAP *BackgroundLayer;
		BITMAP *CharacterLayer;
		BITMAP *ForegroundLayer;

		int MapHeight, MapWidth;

		//need to implement a tile system.
		//maybe a 2d array of structs: Tile

	public:

};



TownMap AltonHouse1;
